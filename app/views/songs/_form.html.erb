<%= form_for @song  do |f| %>
<%
=begin%>
 <%= f.hidden_field :artist_id %> 
<%
=end%>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :artist_name %>
  <%= f.collection_select :artist_id, Artist.all, :id, :name %>
  <br />

  <%= f.submit %>
<% end %>
